export interface NightModeSettings {
  enabled: boolean;
  startTime: string;
  endTime: string;
  temperature: number;
}

export interface ArrivalModeSettings {
  enabled: boolean;
  arrivalTime: string;
  temperature: number;
}